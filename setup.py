from setuptools import setup, find_packages

setup(
    name="emouvi",
    version="0.0.1",
    packages=["module.emouvi"],
    # packages=find_packages(), #does not work why ?
    # entry_points={
    #     "console_scripts": [
    #         "emouvi=", #to install package usually
    #     ],
    # },
)